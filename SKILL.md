---
name: case-framework
description: Enforce or bootstrap the Context-Aware Scaffold Engine (C.A.S.E.) protocol, a file-as-state dual-track agent workflow. Make sure to use this skill whenever the user mentions code quality, task pipelines, roadmaps, step-by-step planning, state tracking, worker-checker verification, or wants to structure their project workflow, even if they don't explicitly name 'C.A.S.E.'
---

# 🤖 C.A.S.E. Framework — Agent Skill Instructions

You are equipped with the **C.A.S.E. (Context-Aware Scaffold Engine)** framework skill. This is a generic, universal project management and quality control protocol that enforces a file-as-state, three-layer architecture to keep AI execution disciplined and trace-transparent.

---

## 🎯 Triggers
This skill triggers and its instructions are loaded when the user mentions:
- "C.A.S.E.", "C.A.S.E. framework", or "scaffold engine"
- "bootstrap C.A.S.E." or "initialize task queue"
- "file-as-state", "three-layer architecture", or "dual-track verification"
- Enforcing rules or verifiers inside the `02_Task_Queue/`

---

## 🏗️ The Three-Layer Architecture

When this skill is active, you must enforce the following folder structure in the workspace:

| Directory | Read/Write | Authority | Contents |
|-----------|------------|-----------|----------|
| `00_Constitution/` | READ-ONLY | Human Architect | Mission constraints ([core.md](file:///D:/Myproject/Local-Agent-Workspace/C.A.S.E._Framework/00_Constitution/core.md)), Memory ([learnings.md](file:///D:/Myproject/Local-Agent-Workspace/C.A.S.E._Framework/00_Constitution/learnings.md)) |
| `01_Roadmap/` | READ-ONLY | Macro-Planner | Milestones ([roadmap.md](file:///D:/Myproject/Local-Agent-Workspace/C.A.S.E._Framework/01_Roadmap/roadmap.md)), Final Acceptance ([global_dod.md](file:///D:/Myproject/Local-Agent-Workspace/C.A.S.E._Framework/01_Roadmap/global_dod.md)) |
| `02_Task_Queue/` | READ-WRITE | Micro-Executor | Self-contained task packages: `Task_<NNN>_<slug>/` |

---

## 📋 Task Package Schema

Every task folder in `02_Task_Queue/` must contain:
1. `status.txt`: State machine token (`PENDING`, `IN_PROGRESS`, `REVIEW`, `DONE`, `ESCALATED`).
2. `role.md`: System prompt override for this specific task.
3. `recipe.md`: Step-by-step instructions and local Definition of Done (DoD).
4. `planning.md`: The micro-plan (BDD scenarios, compaction capsule).
5. `inputs/`: Input data or links to prior task outputs.
6. `output.md`: Primary deliverable generated by the worker.
7. `feedback.md`: Audit feedback written only by Checker or Human.
8. `action_log.jsonl` or `log.md`: Audit trail logging every tool execution.

---

## 🔧 Action Instructions for the Agent

### 0. How to Install or Update C.A.S.E. Skill
To install C.A.S.E. as a standard skill:
* **Global Install** (make it available to all local projects):
  ```bash
  git clone https://github.com/Chiakai-Chang/Local-Agent-Workspace.git ~/.gemini/config/skills/case-framework
  ```
* **Local Project Install** (Workspace Skill):
  ```bash
  git clone https://github.com/Chiakai-Chang/Local-Agent-Workspace.git .agents/skills/case-framework
  ```
* **To Update**: Navigate to the installed folder and run standard git pull:
  ```bash
  git pull
  ```

### 1. How to Bootstrap C.A.S.E. inside a project
If the user asks to "bootstrap", "deploy", or "initialize" C.A.S.E. scaffolding, run the cross-platform Python bootstrap script:
```bash
# Recommended (cross-platform and shell-agnostic):
python scripts/bootstrap.py .

# Alternatively (POSIX/Windows specific shells):
bash scripts/bootstrap.sh .
powershell scripts/bootstrap.ps1 .
```
This script will automatically generate the folders, templates, `MAP.md`, `CASE.md`, and configure gitignore.

### 2. How to Execute a Task in the Queue
If you are assigned to a task inside `02_Task_Queue/`:
1. **Transition**: Set `status.txt` to `IN_PROGRESS` using native file write APIs.
2. **Plan**: Write `planning.md` defining Given-When-Then test cases.
3. **Execute**: Modify files ONLY within the recipe boundaries. Log every tool call to `action_log.jsonl` (or write markdown bullets in `log.md` if using a weaker model).
4. **Self-Heal**: Run tests to verify changes. You have a **maximum of 3 consecutive self-healing attempts** to fix test failures before you must set state to `ESCALATED` and raise an issue.
5. **Submit**: Transition `status.txt` to `REVIEW` and wait for Checker review.

### 3. How to Verify/Audit a Task
If acting as a **Checker**:
1. Run the Node.js or Python verifier (whichever runtime is available) to check the task package integrity:
   ```bash
   # Node.js option:
   node verifiers/verify.js 02_Task_Queue/Task_<NNN>_<slug>

   # Python option:
   python verifiers/verify.py 02_Task_Queue/Task_<NNN>_<slug>
   ```
2. Inspect `output.md` against `recipe.md` DoD.
3. Approve by transitioning state to `DONE` (and perform a git commit), or reject by transitioning back to `PENDING` with feedback in `feedback.md`.

#### Verifier flags

| Flag | Effect |
|------|--------|
| `--strict` | Treat warnings as failures. Ten of the fifteen checks are warnings by default, so a task with no `action_log.jsonl`, no `## Local Definition of Done`, no `planning.md` and a one-character `output.md` still exits 0. Use `--strict` when the whole protocol is meant to be enforced; the default stays permissive so task queues written before a given rule keep their exit codes. |
| `--queue <02_Task_Queue>` | Check the invariants that span the queue instead of one task package: **at most one task `IN_PROGRESS`**, and tasks finished in order. Working one task at a time is what the queue is for, and no per-task check can see a second task running beside it. |
| `--tier-memory` | Run memory tiering after a successful task verification. **This no longer happens automatically.** It used to run on every `DONE`/`REVIEW` verification, which meant a command named `verify` rewrote `00_Constitution/learnings.md` as a side effect, and running it twice did not mean the same thing as running it once. |

```bash
# One task, fully enforced
python verifiers/verify.py 02_Task_Queue/Task_007_Thing --strict

# The queue as a whole
python verifiers/verify.py --queue 02_Task_Queue --strict

# Tiering, when you actually want it to write
python verifiers/verify.py 02_Task_Queue/Task_007_Thing --tier-memory
```

Both verifiers are covered by `tests/test_verify.py` (`python -m unittest discover -s tests`, no dependencies), including a parity check that runs the Python and Node implementations over the same fixtures and fails if they ever disagree about pass/fail. A verifier that lags the protocol lets every newly mandated step go silently unchecked, and there are two of them.

---

## 🌐 4. Cross-Platform & Cross-Shell Intelligent Adaptation Rules
Since C.A.S.E. is a declarative protocol, you (the AI Agent) are the primary executor. You MUST adapt to the host OS and shell intelligently:
- **Tool-First File Mutation**: Always use your native high-level file tools (e.g. `write_to_file`, `replace_file_content`) to change files like `status.txt`, `action_log.jsonl`, `planning.md`. **NEVER** run host shell redirection commands (e.g. `echo "IN_PROGRESS" > status.txt`) as they vary by shell and can pollute encodings (like UTF-16 in PowerShell).
- **Standard UTF-8 & LF**: All files written by you MUST use UTF-8 (without BOM) and LF (`\n`) line endings.
- **Dynamic Path Slashes**: Standardize command paths using forward slashes (`/`) even on Windows, as Node/Python and Windows file APIs natively support them.

---

## 📚 Reference Documentation
For deep-dives, read these sub-files (located in the `references/` folder):
* [System Protocols for Agents](file:///D:/Myproject/Local-Agent-Workspace/references/for_agents.md) (Relative: [for_agents.md](references/for_agents.md))
* [Instruction Guide for Humans](file:///D:/Myproject/Local-Agent-Workspace/references/for_humans.md) (Relative: [for_humans.md](references/for_humans.md))
* [Harness & Runtime Engineering Specs](file:///D:/Myproject/Local-Agent-Workspace/references/harness_engineering.md) (Relative: [harness_engineering.md](references/harness_engineering.md))
* [Zero-Code Portable Integration](file:///D:/Myproject/Local-Agent-Workspace/references/portable_case_harness.md) (Relative: [portable_case_harness.md](references/portable_case_harness.md))
* [Glossary of C.A.S.E. Terms](file:///D:/Myproject/Local-Agent-Workspace/references/glossary.md) (Relative: [glossary.md](references/glossary.md))
